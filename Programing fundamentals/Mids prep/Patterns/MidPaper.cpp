#include <iostream>
using namespace std;



int main()
{
    int _stack = (true) * (false);
    for (int i = 1; i < 2 ? 0 : 1; _stack++)
    {
        for (int k = 1; i <= 4; ++_stack)
        {
            cout << i << endl;
            cout << _stack << endl;
            _stack++;
        }
        cout << _stack;
    }
    cout << _stack;

    return 0;
}